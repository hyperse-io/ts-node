---
"@hyperse/ts-node": minor
---

Support tsconfig without `baseUrl` (TypeScript 6.0), normalize inherited preset `baseUrl`, and default `TsConfig.getCompilerOptions()` resolution to the tsconfig directory.
